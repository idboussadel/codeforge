{
  "name": "@codeforge/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cd src && PYTHONPATH=. ../venv/bin/python -m uvicorn codeforge.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "cd src && PYTHONPATH=. ../venv/bin/python -m uvicorn codeforge.main:app --host 0.0.0.0 --port 8000"
  }
}
